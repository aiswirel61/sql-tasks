Create a SQL query to calcualte total orders count by each day in 1998:

| Order Date | Total Number of Orders |

SELECT FORMAT(OrderDate, 'yyyy-MM-dd') as [Order Date], COUNT(OrderID) as [Total Number of Orders] FROM Orders WHERE YEAR(OrderDate) = 1998 GROUP BY OrderDate
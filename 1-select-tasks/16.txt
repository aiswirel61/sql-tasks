Create a SQL query to return all orders where ship postal code is provided:

| OrderId | CustomerId | ShipCountry |

SELECT OrderId, CustomerId, ShipCountry FROM Orders WHERE ShipPostalCode IS NOT NULL